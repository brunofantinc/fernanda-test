---
import { getCollection } from "astro:content";

const allArticles = await getCollection("articles");
const latestArticles = allArticles
  .sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime())
  .slice(0, 3);

const formatDate = (dateStr: string) =>
  new Date(dateStr).toLocaleDateString("pt-BR", { day: "numeric", month: "long", year: "numeric" });

const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');
const truncate = (str: string, max: number) => str.length > max ? str.slice(0, max) + '...' : str;
---

<section id="artigos" class="py-16 bg-white">
  <div class="max-w-7xl mx-auto px-6 text-center">
    <h2 class="text-3xl font-bold uppercase text-[#313d25] mb-8">
      Artigos Recentes
    </h2>

    <div class="flex flex-wrap justify-center gap-8 text-left">
      {latestArticles.map((article) => (
        <a
          href={`${base}articles/${article.slug}`}
          class="group bg-gray-50 rounded-2xl p-6 shadow-sm hover:shadow-md transition flex flex-col w-full md:w-[30%]"
        >
          <p class="text-xs text-gray-400 mb-2 uppercase tracking-wider">{formatDate(article.data.date)}</p>
          <h3 class="text-lg font-bold text-[#313d25] mb-3 group-hover:text-[#313d25] transition">
            {truncate(article.data.title, 80)}
          </h3>
          <p class="text-gray-600 text-sm flex-1">{article.data.description}</p>
          <span class="mt-4 text-[#0B2545] font-semibold text-sm group-hover:text-[#F9C74F] transition">
            Leia mais →
          </span>
        </a>
      ))}
    </div>

    <a
      href={`${base}articles`}
      class="inline-block mt-10 px-8 py-3 bg-[#0B2545] text-white rounded-full font-semibold hover:bg-[#F9C74F] hover:text-[#0B2545] transition"
    >
      Ver todos os artigos
    </a>
  </div>
</section>
