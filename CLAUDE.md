# Fernanda Test — Project Rules

## Git Workflow

- Always make changes directly in the main working directory (`C:\Users\Bruno\Desktop\fernanda-test`).
- When creating a new branch, use `git checkout -b <branch>` inside the main repo — never use git worktrees unless explicitly instructed.
- This ensures all changes are immediately visible at `http://localhost:4321/`.
